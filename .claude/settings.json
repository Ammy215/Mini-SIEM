{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys, json, datetime; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path') or d.get('tool_input',{}).get('notebook_path'); f and open(r'c:/Users/Abbas/Mini siem/CHANGELOG_CLAUDE.md','a').write(datetime.datetime.now(datetime.timezone.utc).strftime('%Y-%m-%dT%H:%M:%SZ')+'\\t'+f+'\\n')\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
