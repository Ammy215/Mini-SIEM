# Leave empty: the app calls its own origin and /api is proxied to the backend
# (vite.config.js locally, vercel.json in production). Only set this to point a
# build at a backend on a different origin, which brings back cross-site cookies.
VITE_API_BASE_URL=
